[project]
name = "odes-and-sdes"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "numpy",
    "matplotlib",
    "seaborn",
    "tqdm",
    "celluloid",
    "jupyter",
]
